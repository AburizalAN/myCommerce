import { makeStyles } from "@material-ui/core/styles";

export default makeStyles(() => (
    {
        flex: {
            display: "flex",
            justifyContent: "space-between"
        }
    }
))